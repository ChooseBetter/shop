export interface UserState {
  profile: object;
}
export interface goodType {
  id: string;
  skuId: string;
  name: string;
  attrsText: string;
  picture: string;
  price: string;
  nowPrice: string;
  selected: boolean;
  stock: number;
  count: number;
  isEffective: boolean;
}
